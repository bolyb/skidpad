package skidpadprogram;

import java.io.File;
import java.io.FileNotFoundException;
import java.io.IOException;
import java.util.ArrayList;
import java.util.List;
import java.util.Scanner;

/**
 *
 * @author Brian
 */
public class Main {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) throws FileNotFoundException, IOException {
        /*Scanner reader = new Scanner(System.in);
        Process p = new Process();
        System.out.println("Enter the max value to determine if a lap time is an invalid lap");
        p.setMax(reader.nextDouble());
        System.out.println("Ensure that the files are named list1.csv and list2.csv: ");
        p.findev1();
        p.findev2();
        p.findav1();
        p.findav2();
        p.makeList1();
        p.makeList2();
        System.out.println("List 1 with outliers removed: " + p.getList1());
        System.out.println("List 2 with outliers removed: " + p.getList2());
        System.out.println("List 1 evens with outliers removed: " + p.getList1ev());
        System.out.println("List 2 evens with outliers removed: " + p.getList2ev());
        System.out.println("List 1 averages of 2nd and 4th laps: " + p.getList1av());
        System.out.println("List 2 averages of 2nd and 4th laps: " + p.getList2av());
        System.out.println("List 1 min: " + p.getMin1());
        System.out.println("List 2 min: " + p.getMin2());
        System.out.println("List 1 score: " + p.getScore1());
        System.out.println("List 2 score: " + p.getScore2());*/
    }
}
